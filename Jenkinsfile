pipeline {
    // agent { dockerfile true }
    agent any
    stages {
        stage('Test') {
            steps {
                sh 'docker --version'
                // sh 'svn --version'
            }
        }
    }
}